const sumMe = {
  hello: 'there',
  you: 8,
  are: 7,
  almost: '10',
  done: '!'
};
let total = 0;
// ADD CODE HERE

for (let prop in sumMe) {
  if (typeof sumMe[prop] === 'number') {
    total += sumMe[prop];
  }
}

console.log(total);
